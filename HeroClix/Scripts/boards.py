def _swap_board(board):
    mute()
    table.board = board
    
    
def swap_board_default(group, x=0, y=0):
    mute()
    _swap_board("")


def swap_board_dc_badges(group, x=0, y=0):
    mute()
    _swap_board("DC Badges")
    
    
def swap_board_jla(group, x=0, y=0):
    mute()
    _swap_board("JLA")
    
    
def swap_board_xmen_collage(group, x=0, y=0):
    mute()
    _swap_board("X-Men Collage")
    
    
def swap_board_spiderweb(group, x=0, y=0):
    mute()
    _swap_board("Spiderweb")


def swap_board_avengers_collage(group, x=0, y=0):
    mute()
    _swap_board("Avengers Collage")


def swap_board_hulkbuster(group, x=0, y=0):
    mute()
    _swap_board("Hulk and Hulkbuster")

